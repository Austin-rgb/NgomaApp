<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NgomaApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

    <service
            android:name=".UtilityService"
            android:enabled="true"
            android:exported="false"
            android:permission="TODO" />

        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ClassesTableActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SubjectsTableActivity" />
        <activity android:name=".TopicsTableActivity" />
        <activity android:name=".QuestionsTableActivity" />
        <activity android:name=".AnswerActivity" />
        <activity android:name=".SetupActivity" />
    </application>

</manifest>